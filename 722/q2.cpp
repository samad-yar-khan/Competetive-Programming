#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main()
{
    ios_base::sync_with_stdio(false);
 
    cin.tie(NULL);
 

    ll T{1};
    cin>>T;
    while(T--){

        ll n{0} ;
        cin>>n;
        ll arr[100000];
        for(ll i = 0 ; i < n ; i++){
            cin>>arr[i];
        }

        sort(arr , arr+n);

    }

}